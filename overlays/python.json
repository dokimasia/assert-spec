{
  "extends": "spec://assertions@1.0.0",
  "language": "python",
  "diverge": [
    {
      "id": "bench-max-allocs",
      "stance": "blocked",
      "why": "CPython counts no allocations. Every primitive answers a level of live memory rather than a running total: tracemalloc.get_traced_memory gives current and peak bytes, sys.getallocatedblocks gives the blocks alive right now, and a tracemalloc snapshot counts the blocks that survived. A body allocating ten blocks and freeing them each iteration reads the same as one that allocates nothing.",
      "remedy": "A cumulative allocation counter in CPython, of the kind runtime.MemStats.Mallocs gives Go."
    }
  ],
  "surface": [
    {
      "id": "reporter",
      "why": "The seat states report and clock itself, with a default, so a seat that supplies neither gets the platform clock and a rendered sentence. Only a language whose interfaces carry no defaults needs a second interface to say the same thing."
    },
    {
      "id": "clocked",
      "why": "The seat states report and clock itself, with a default, so a seat that supplies neither gets the platform clock and a rendered sentence. Only a language whose interfaces carry no defaults needs a second interface to say the same thing."
    },
    {
      "id": "contract.measuring",
      "why": "A benchmark body here is inline, driven by a generator, so a setup closure would take the caller out of the loop they are already writing. It marks the setup in place with excluding instead."
    },
    {
      "id": "assertion",
      "why": "The static form already reads as a chain would: an assertion takes the value positionally and nothing is repeated but the seat. A chain buys type safety in a language that checks types at compile time, and Python checks none."
    },
    {
      "id": "that",
      "why": "The static form already reads as a chain would: an assertion takes the value positionally and nothing is repeated but the seat. A chain buys type safety in a language that checks types at compile time, and Python checks none."
    }
  ]
}
