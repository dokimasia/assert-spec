{
  "extends": "spec://assertions@1.0.0",
  "language": "rust",
  "diverge": [],
  "relaxations": [
    {
      "id": "equate-empty",
      "why": "An absent container and an empty one are different types in Rust: Option<Vec<T>> is not Vec<T>. There is no case where a caller has one and means the other, so a relaxation would widen nothing."
    },
    {
      "id": "equate-nans",
      "why": "Rust's == already says NaN is unequal to itself, which is what the standard states by default. A caller wanting the other answer compares f64::to_bits or names the case, and a relaxation reaching inside a generic T: PartialEq is not expressible without a comparison routine this implementation does not have."
    }
  ],
  "limits": [
    {
      "id": "bench-max-allocs",
      "what": "Counts allocations exactly, once CountingAllocator is installed as the test binary's global allocator. Without it the assertion reports that nothing counted rather than passing.",
      "why": "Rust can count every allocation, which no other implementation of this standard can. What it cannot do is install a global allocator on a caller's behalf: a program has one, the program chooses it, and a library that took it would take it from the caller."
    },
    {
      "id": "bench-max-bytes",
      "what": "Counts bytes requested, once CountingAllocator is installed as the test binary's global allocator. Without it the assertion reports that nothing counted rather than passing.",
      "why": "The same allocator answers both ceilings, so both stand or fall on it being installed. The count is of bytes asked for rather than bytes resident, which is what makes it stable across runs."
    },
    {
      "id": "no-task-leaks",
      "what": "Sees tasks spawned on the Tokio runtime the test is running under. A thread started with std::thread is not seen.",
      "why": "Rust's standard library has no way to enumerate threads: nothing in std::thread lists them, so the synchronous crate cannot answer the question at all. Tokio counts its own live tasks, which is why this assertion ships in the tokio crate rather than the core."
    }
  ],
  "surface": [
    {
      "id": "reporter",
      "why": "The seat states report and clock itself, with a default, so a seat that supplies neither gets the platform clock and a rendered sentence. Only a language whose interfaces carry no defaults needs a second interface to say the same thing."
    },
    {
      "id": "clocked",
      "why": "The seat states report and clock itself, with a default, so a seat that supplies neither gets the platform clock and a rendered sentence. Only a language whose interfaces carry no defaults needs a second interface to say the same thing."
    },
    {
      "id": "contract.excluding",
      "why": "Go writes a benchmark body inline under testing.B, so a setup closure would take the caller out of both the loop and the chain. It marks the setup in place with Excluding instead."
    }
  ]
}
