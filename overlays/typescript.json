{
  "extends": "spec://assertions@1.0.0",
  "language": "typescript",
  "diverge": [
    {
      "id": "bench-max-allocs",
      "stance": "blocked",
      "why": "V8 exposes no per-iteration allocation count. Node offers heap totals through process.memoryUsage and v8.getHeapStatistics, and neither counts allocations, so there is no number to hold to a ceiling.",
      "remedy": "A counter in V8's API, or a profiler sampling allocations, which is too slow to run per iteration."
    },
    {
      "id": "bench-max-bytes",
      "stance": "blocked",
      "why": "Bytes allocated per iteration can only be read as a heap-usage delta, which moves with whether the collector ran. Measuring one unchanged body six times gave 43, -3, 43, -10, -10 and -8 bytes per iteration, so a ceiling set from one run fails the next for no reason.",
      "remedy": "A per-iteration allocation total that does not depend on collector timing."
    }
  ],
  "limits": [
    {
      "id": "pure",
      "what": "Accepts no relaxations, where the other implementations let a caller equate NaNs or an absent container with an empty one for this call.",
      "why": "An oversight rather than a decision: equal, notEqual, contains and notContains all take them here and isPure does not. It is recorded rather than fixed quietly so the gap is visible until it closes."
    },
    {
      "id": "honours-cancellation",
      "what": "Reports no call site. The assertion awaits the subject, and by the time it reports, the caller's frame has left the stack.",
      "why": "V8 gives an async function no synchronous caller frame once it has awaited. The standard states a location as optional for this reason, and every assertion that does not await carries one."
    },
    {
      "id": "recorder-seat",
      "what": "Carries no lock. The runtime has one thread, so two assertions cannot report at the same instant.",
      "why": "The standard asks that a seat be usable from more than one thread. A runtime with one thread meets that without doing anything, and says so here rather than leaving a reader to work out which it is."
    }
  ],
  "surface": [
    {
      "id": "reporter",
      "why": "The seat states report and clock itself, with a default, so a seat that supplies neither gets the platform clock and a rendered sentence. Only a language whose interfaces carry no defaults needs a second interface to say the same thing."
    },
    {
      "id": "clocked",
      "why": "The seat states report and clock itself, with a default, so a seat that supplies neither gets the platform clock and a rendered sentence. Only a language whose interfaces carry no defaults needs a second interface to say the same thing."
    },
    {
      "id": "contract.excluding",
      "why": "Go writes a benchmark body inline under testing.B, so a setup closure would take the caller out of both the loop and the chain. It marks the setup in place with Excluding instead."
    },
    {
      "id": "assertion",
      "why": "equal and notEqual hold want to the type of got through NoInfer, so the static form already rejects a mismatched pair at compile time. A chain would be a second way to write what is checked either way."
    },
    {
      "id": "that",
      "why": "equal and notEqual hold want to the type of got through NoInfer, so the static form already rejects a mismatched pair at compile time. A chain would be a second way to write what is checked either way."
    }
  ]
}
